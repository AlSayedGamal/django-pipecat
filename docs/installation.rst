============
Installation
============

At the command line::

    $ easy_install django-pipecat

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-pipecat
    $ pip install django-pipecat
